# Hướng dẫn đóng góp (Contributing Guidelines)

Cảm ơn bạn đã quan tâm và muốn đóng góp cho `flutter_cache_avatar`! 

Để đảm bảo chất lượng của mã nguồn và tính ổn định của thư viện, quy trình đóng góp sẽ diễn ra như sau:

## 1. Báo cáo lỗi (Report Bugs)
Nếu bạn phát hiện ra lỗi (bug) trong quá trình sử dụng, vui lòng mở một **Issue** trên GitHub.
Vui lòng cung cấp:
- Phiên bản Flutter bạn đang dùng.
- Đoạn code gây ra lỗi.
- Thông báo lỗi chi tiết (nếu có).

## 2. Đề xuất tính năng (Feature Requests)
Nếu bạn có ý tưởng về một tính năng mới, hãy mở một **Issue** để chúng ta cùng thảo luận trước khi bạn bắt tay vào code. Điều này giúp đảm bảo tính năng đó phù hợp với định hướng của thư viện.

## 3. Đóng góp mã nguồn (Pull Requests)
Kho lưu trữ này không cho phép người ngoài đẩy (push) code trực tiếp. Để đóng góp code, bạn vui lòng làm theo các bước sau:
1. **Fork** repository này về tài khoản GitHub của bạn.
2. Clone bản fork đó về máy tính.
3. Tạo một nhánh (branch) mới cho tính năng hoặc bản vá lỗi của bạn: `git checkout -b feature/ten-tinh-nang`.
4. Viết code và đảm bảo code của bạn không gây ra lỗi (`flutter analyze` trả về 0 issues).
5. Push branch đó lên bản fork của bạn.
6. Mở một **Pull Request (PR)** từ bản fork của bạn vào nhánh `main` của repository gốc.
7. Đợi tôi (chủ repository) review và merge code.

Cảm ơn bạn rất nhiều!
